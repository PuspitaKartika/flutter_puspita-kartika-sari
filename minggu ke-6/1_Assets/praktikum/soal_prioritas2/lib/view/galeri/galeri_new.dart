import 'package:flutter/material.dart';

class GaleriNewScreen extends StatelessWidget {
  const GaleriNewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("New Galeri"),
      ),
      body: ListView(),
    );
  }
}
